# 고객사 판매 전환을 위한 우선순위 로드맵

작성일: 2026-08-26 · 대상: MAJU Intelligence를 단일 회사 전용 운영에서 여러 고객사에 파는 멀티테넌시 SaaS로 전환

## 요청 항목 현황 요약

| # | 요청 항목 | 현재 상태 |
|---|---|---|
| 1 | 로그인 화면 | 있음 (기능함), 단 실제 계정정보가 하드코딩 노출되어 있던 버그는 오늘 수정·배포함 |
| 2 | 로그인 및 회원가입 | ✅ **완료** — `/signup`에서 회사가 스스로 가입 가능(회사명·사업자번호·담당자·이메일·비밀번호) |
| 3 | 비밀번호 찾기 | **전혀 없음** — 이메일/문자 발송 인프라조차 없음 (P1 예정) |
| 4 | 카카오 로그인 관리 | 코드는 완성되어 있음(카카오/네이버/구글 3사 모두). CSRF state 검증도 추가 완료. 실제 서비스 심사·환경변수 세팅만 남음 |
| 5 | 회사 계정 유일성 + 직원 가입 + 관리자의 고객사 관리 | ✅ **완료** — 사업자등록번호 유일 인덱스 추가(마이그레이션 실행 대기). 직원 초대·관리자의 고객사 관리 화면은 기존에 이미 구현되어 있었음 |
| 6 | 약관/동의서 | ✅ **완료** — `/legal/terms`, `/legal/privacy` + 가입 시 필수 동의 체크박스 (연락처 등은 placeholder, 실제 정보로 교체 필요) |
| 7 | 결제(사용후결제/자동결제) | **전혀 없음** — PG 연동, 구독 데이터 모델 모두 처음부터 구축 필요 |

요약하면: 이 앱은 원래 "우리 회사 한 곳"을 위해 만들어졌고, 그 뼈대 위에 카카오/네이버/구글 로그인과 직원 초대, 관리자용 고객사 관리 화면까지는 이미 잘 만들어져 있습니다. 지금 진짜로 비어있는 것은 **① 신규 회사가 스스로 가입하는 화면**, **② 비밀번호 찾기**, **③ 약관 동의**, **④ 결제** 네 가지이고, 그 전에 반드시 손봐야 하는 구조적 문제가 하나 있습니다 — 지금 코드 곳곳이 "회사가 하나뿐"이라는 가정으로 짜여 있어서, 그대로 여러 회사에 팔면 데이터가 섞이거나 새어나갈 위험이 있습니다(아래 P0-1).

---

## 🔴 즉시 수정·배포 완료한 것

- **로그인 화면에 실제 계정 이메일과 비밀번호가 기본값으로 박혀 있던 문제.** 개발 중 편의를 위해 넣어둔 것으로 보이는데, 프로덕션에 그대로 배포되어 있어서 로그인 페이지 소스코드만 봐도 실제 이메일 주소와 비밀번호가 노출되는 상태였습니다. 빈 칸으로 수정했습니다.
- 카카오 로그인 콜백에서 로그인 세션 저장 함수 호출에 `await`가 빠져 있어, 드물게 쿠키가 다 저장되기 전에 리다이렉트가 발생할 수 있던 버그를 네이버/구글 콜백과 동일하게 수정했습니다.
- **(P0-2) 비밀번호 평문 저장·비교 → 해시(scrypt) 전환 완료.** 관리자 계정, 회사별 고객 계정 모두 로그인 시 자동으로 해시로 전환되도록 만들었고(비밀번호를 다시 입력받을 필요 없음), 새로 만드는 계정은 처음부터 해시로 저장됩니다. **다음 로그인 때 정상적으로 로그인되는지 한 번만 확인 부탁드립니다** — 비밀번호 검증 로직 자체를 바꾼 것이라 실제 로그인으로 확인하는 게 가장 확실합니다. 참고로 이후 관리자 설정 화면(계정 관리)에서 비밀번호 칸에는 실제 비밀번호 대신 해시 값(알아볼 수 없는 문자열)이 보이는데, 이건 정상입니다 — 저장된 비밀번호를 다시 평문으로 볼 수 없게 하는 게 해시화의 목적입니다.

---

## P0 — 여러 회사에 팔기 전 반드시 끝내야 하는 것 (구조적 전제조건)

이 항목들을 건너뛰고 회원가입·결제부터 만들면, 나중에 되돌리기 훨씬 어려운 구조적 문제가 됩니다.

1. ✅ **완료(재평가) — 멀티테넌시 안전성 전수 조사 + 방어 강화.** `lib/store.ts`의 companyId 관련 폴백 패턴 65곳을 전부 추적한 결과, 다행히 실제로 뚫려있는 경로는 없었습니다 — 지금 있는 API 라우트·페이지는 전부 로그인 인증(`getRequestAuthScope`)이나 페이지 이동 가드로 이미 companyId를 강제하고 있습니다. 다만 5개 함수(`getLatestReport`, `getReportById`, `getLatestLeads`, `getVisitTimeline`, `updateLeadStatus`)는 companyId가 없을 때 "회사 필터 자체가 빠지는" 구조라 — 나중에 인증 검증을 빠뜨린 코드가 추가되면 그 즉시 전체 회사 데이터가 노출/오염될 수 있는 잠재 위험이었습니다. 이 5곳을 "companyId 없으면 아예 실행하지 않음"으로 방어 코드를 넣어 배포했습니다. (관리자 전용 "전체 회사 보기" 화면 2곳은 의도된 동작이라 그대로 뒀습니다.)
2. ✅ **완료 — 비밀번호 해시 전환.** scrypt 해시로 전환했고, 기존 계정도 다음 로그인 시 자동 마이그레이션됩니다.
3. ✅ **완료 — 회사 자체 가입(온보딩) 화면.** `/signup` 페이지를 새로 만들었습니다. 회사명·사업자등록번호·담당자명·이메일·비밀번호를 입력받고, 가입 즉시 자동 로그인되어 대시보드로 이동합니다. `/dashboard/login`에 "처음이신가요? 회사 가입하기" 링크를 추가했습니다. 기존 (b) 카카오/네이버/구글 초대코드 없는 개인 워크스페이스 자동 생성 동작은 그대로 남겨뒀습니다 — 이건 "직원이 초대 없이 소셜 로그인만으로 임시 개인 작업공간을 갖는" 별개의 용도라 건드리지 않았고, 이제 정식 회사 계정은 `/signup`으로 만드는 경로가 별도로 생겼습니다.
4. ✅ **완료 — 사업자등록번호 중복 가입 방지.** `companies` 테이블에 `business_registration_number` 컬럼과 부분 유일 인덱스(값이 있을 때만 중복 차단)를 추가하는 마이그레이션을 작성했습니다. 가입 API에서도 저장 전에 미리 조회해 "이미 가입된 사업자등록번호입니다" 같은 친절한 한글 오류를 보여주고, DB 유일 인덱스가 최종 안전장치 역할을 합니다. **⚠️ 아래 "실행이 필요한 작업" 참고 — 이 마이그레이션은 Supabase에서 아직 실행되지 않았습니다.**
5. ✅ **완료 — 이용약관·개인정보처리방침 + 동의 UI.** `/legal/terms`, `/legal/privacy`에 최소 요건을 갖춘 문서를 작성했고, `/signup` 가입 폼에 두 문서에 대한 필수 동의 체크박스를 추가했습니다(서버에서도 동의 여부를 검증해 체크 안 하면 가입이 거부됩니다). **⚠️ 두 문서 모두 사업자명·주소·대표자·고객센터 연락처 등은 `[대괄호]` placeholder로 남겨뒀습니다** — 실제 서비스 오픈 전 실제 정보로 채우고 법률 검토를 받으시기를 권장합니다(문서 상단에도 같은 안내를 넣어뒀습니다).
6. ✅ **완료 — OAuth state 값 CSRF 검증.** 카카오/네이버/구글 로그인 시작 시 매번 임의의 nonce를 발급해 짧은 수명의 httpOnly 쿠키에 저장하고, 콜백에서 그 값과 실제로 대조하도록 바꿨습니다. 검증에 실패하면(위조되었거나 만료된 요청) 로그인을 진행하지 않고 안내 화면으로 돌려보냅니다. 기존에 `state`가 초대코드를 실어 나르던 동작은 그대로 유지했습니다(nonce 뒤에 붙여서 전달).

## P1 — 서비스 오픈 전 마무리 (사용자 요청 항목 완결)

- ✅ **완료 — 직원 초대 권한 세분화.** 대표(owner)·관리자(manager)만 직원을 초대·역할변경·비활성화할 수 있도록 제한했습니다(`lib/workspace.ts`의 `manage_members`). 다른 직급이 회사 설정 화면에 들어가면 안내 문구만 보이고 폼 자체가 비활성화됩니다.
- **카카오/네이버/구글 로그인 실제 서비스 전환 — 사용자 조치 대기.** 코드는 이미 3사 모두 완성되어 있습니다. 남은 건 순수 설정 작업입니다: 각 플랫폼(카카오 디벨로퍼스, 네이버 커넥트, 구글 클라우드) 콘솔에 앱 등록 → 리다이렉트 URI(`https://maju-intelligence.vercel.app/api/auth/{kakao|naver|google}/callback`) 등록 → 발급받은 키를 Vercel 환경변수에 입력. 구글은 개인정보처리방침 URL로 `https://maju-intelligence.vercel.app/legal/privacy`를 입력하면 됩니다(이제 준비됨).
- ✅ **완료 — 비밀번호 찾기 플로우.** `/forgot-password`(이메일 입력) → `/reset-password?token=...`(새 비밀번호) 화면과 API를 만들었습니다. 재설정 토큰은 해시로만 저장되고 30분 후 만료됩니다. **다만 실제 이메일 발송은 Resend API 키가 있어야 동작합니다** — `RESEND_API_KEY`가 Vercel에 없으면 토큰은 정상 발급되지만 이메일이 나가지 않고 서버 로그에만 경고가 남습니다. [resend.com](https://resend.com)에서 무료로 API 키를 발급받아 Vercel 환경변수 `RESEND_API_KEY`(필요시 `RESEND_FROM_EMAIL`도)에 넣어주세요.
- **관리자(MAJU)의 고객사 관리 화면 고도화 — P2 이후 진행 권장.** 여러 회사를 한 눈에 보고 관리하는 화면 자체는 이미 있습니다. 다만 회사별 요금제·결제 상태·이용 현황까지 이 화면에서 보이도록 P2(결제) 항목과 함께 확장해야 하므로, 결제 데이터 모델이 먼저 정해져야 합니다.

## P2 — 결제 시스템 (요청 7) — ⏸️ 보류 (사용자 지시)

**2026-08-27 사용자 지시로 보류 상태입니다.** "전반적으로 비용이 들지 않는 방향으로 가야 한다"는 원칙과 함께, 결제 시스템 착수는 별도 지시가 있을 때까지 진행하지 않습니다. 지금은 결제 관련 코드가 전혀 없어서, 완전히 새로 구축하는 항목입니다. 진행 전에 사업적으로 먼저 정해야 할 것이 있습니다.

1. **사용후결제(후불) vs 자동결제(정기 선불) 중 어느 모델로 갈지 결정.** 이 결정에 따라 필요한 시스템이 크게 달라집니다 — 후불은 사용량 집계·정산 로직이 핵심이고, 자동결제(정기구독)는 PG사의 "빌링키" 발급·정기 청구 스케줄링이 핵심입니다. 국내 SaaS는 보통 자동결제(월 구독) 쪽이 익숙하고 구현도 상대적으로 단순합니다.
2. **PG사(결제대행사) 선정.** 국내에서 정기결제(빌링키)를 지원하는 곳은 토스페이먼츠, 포트원(구 아임포트), 나이스페이 등이 있습니다. 수수료·정산주기·빌링키 지원 여부를 비교해 선정이 필요합니다.
3. **구독/플랜/결제상태 데이터 모델 신규 구축.** 지금 회사 테이블에는 요금제나 구독 상태 개념이 전혀 없습니다. 플랜(요금제), 결제주기, 결제상태(정상/연체/해지), 다음 결제일 같은 필드를 새로 설계해야 합니다.
4. **사용량 기반 과금이라면 사용량 집계 시스템도 별도 구축.** (1번에서 사용후결제로 정하는 경우에 한함)

## P3 — 급하지 않지만 알아두어야 할 것

- 데이터베이스 레벨 접근 제어(RLS)가 켜져는 있지만 실제 규칙이 하나도 없어서, 지금은 앱 코드가 회사 ID로 잘 걸러주는지에 전적으로 의존하고 있습니다. P0-1(멀티테넌시 정리)이 끝나면, DB 차원에서도 이중 안전장치를 넣는 걸 권장합니다.
- 로그인 세션의 직급 구분이 두 단계(대표/직원)뿐인데, 실제 직원 권한 체계는 다섯 단계(대표·관리자·영업·배송기사·직원)로 따로 있어 서로 어긋나 있습니다. P1(권한 세분화) 작업 때 같이 정리하면 좋습니다.

---

## ⚠️ 지금 바로 실행이 필요한 작업 (배포는 됐지만 사용자 조치 대기 중)

1. ✅ (완료 확인) **Supabase SQL 마이그레이션 실행.** `20260826_company_self_signup.sql`은 실행 완료를 실제 가입 테스트로 확인했습니다.
2. **신규 — `supabase/migrations/20260826_password_reset.sql` 실행.** `auth_credentials` 테이블에 비밀번호 재설정 토큰 컬럼을 추가하는 마이그레이션입니다. **이걸 실행하기 전까지는 비밀번호 찾기가 실패합니다.**
3. **신규 — Resend API 키 발급 및 Vercel 등록.** [resend.com](https://resend.com) 가입 → API 키 발급 → Vercel 환경변수에 `RESEND_API_KEY` 추가(선택: `RESEND_FROM_EMAIL`). 없으면 비밀번호 재설정 이메일이 발송되지 않습니다.
4. **`/legal/terms`, `/legal/privacy`의 `[대괄호]` 부분을 실제 정보로 채워주세요.** 사업자명, 사업자등록번호, 대표자, 주소, 고객센터 이메일 등입니다. 지금은 최소 템플릿이라 법적으로 완전하지 않을 수 있어, 실제 서비스 오픈 전 법률 검토를 권장드립니다.
5. **카카오/네이버/구글 개발자 콘솔에서 앱 등록 후 키 6종을 Vercel에 등록해주세요.** (카카오 REST 키, 네이버 Client ID/Secret, 구글 Client ID/Secret) — 위 P1 항목에 정확한 리다이렉트 URI를 적어뒀습니다.

## ✅ 완료 — 폴리싱 패스 (2026-08-27)

"지금 만든 로직 다듬어달라"는 요청으로, 새 기능 추가 없이 이번 세션에서 만든 P0/P1 로직을 다시 검토해 아래를 고쳤습니다(모두 비용 없는 순수 코드 개선):

- 사업자등록번호 체크섬 검증 로직이 `app/page.tsx`와 `lib/store.ts`에 각각 따로 있던 것을 `lib/business-number.ts` 하나로 통합.
- 회사 가입 API(`createCompanySignup`)가 자릿수만 확인하고 실제 체크섬은 검증하지 않던 허점을 메움 — 이제 회사 가입 화면과 엑셀 업로드 검증이 정확히 같은 기준으로 사업자등록번호를 검사합니다.
- `/signup` 폼에 실시간 검증(잘못된 사업자등록번호 입력 시 빨간 테두리 + 안내문)을 추가해 서버 왕복 없이 바로 알 수 있게 함.
- 비밀번호 재설정 링크가 만료·오류났을 때 사용자가 다시 요청할 방법이 없던 것을 고쳐, 오류 화면에 "재설정 링크 다시 요청하기" 링크를 추가.
- 직원 초대 관리 화면에서 대표/관리자가 아닌 직원도 대기 중인 초대 링크를 복사할 수 있었던 사소한 구멍을 마저 막음(생성·수정·비활성화는 이미 막혀 있었음).

**작업 중 발견한 것(참고만 해주세요, 이번엔 손대지 않았습니다):**

- 로컬 작업 폴더의 `app/page.tsx`에 이전 세션에서 만들어졌지만 아직 배포되지 않은 UI 변경(파란색→틸색 색상, "운영 리포트"→"AI 리포트" 문구 변경, 이슈 목록 페이지네이션 등)이 남아있습니다. 이번 폴리싱과 무관한 내용이라 건드리지 않았는데, 이걸 배포할지 여부를 알려주시면 다음에 반영하겠습니다.
- 로컬 작업 폴더의 `lib/store.ts`와 관련 API 라우트 2곳에 예전에 제거했던(작업 #74) `localdata.go.kr` 자동 수집 기능 코드가 되살아나 있는 걸 발견했습니다. 실제 배포본(origin)에는 이미 없는 코드라 이번 배포에는 포함하지 않았지만, 로컬 파일과 배포본이 이 부분에서 어긋나 있는 상태입니다. 문제되진 않지만, 완전히 정리해드릴지 그대로 둘지 말씀해주시면 처리하겠습니다.

## 다음으로 진행할 것

P0 전체(1~6), P1 직원 초대 권한 세분화, 비밀번호 찾기, 그리고 위 폴리싱 패스까지 구현·배포 완료했습니다. P2(결제)는 사용자 지시로 보류 중입니다. 남은 항목(OAuth 프로덕션 전환, 관리자 화면 고도화)은 사용자님이 직접 하셔야 할 일(외부 계정 키 발급, 결제 재개 후 설계)이라 제가 코드로 더 진행할 부분이 없습니다. 위 "지금 바로 실행이 필요한 작업" 2~5번을 처리해주시고, 위 두 가지 발견 사항(page.tsx 미배포 UI, store.ts localdata 잔재)에 대한 의견도 알려주세요.
