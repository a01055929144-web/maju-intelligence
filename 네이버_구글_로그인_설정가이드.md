# 네이버 / 구글 로그인 설정 가이드

카카오 로그인과 같은 방식으로, 직원이 네이버 또는 구글 계정으로도 가입/로그인할 수 있도록 화면과 서버 코드는 이미 배포되어 있습니다. 다만 네이버와 구글 각각의 개발자 콘솔에 앱을 등록하고, 발급받은 키를 Vercel에 넣어주는 작업만 남았습니다. 이 작업은 본인 계정으로만 할 수 있어서 직접 진행해주셔야 합니다.

카카오 때와 마찬가지로, 콘솔 화면이 이 가이드와 다르게 보이면 그 화면을 캡처해서 보내주세요. 그 자리에서 맞는 위치를 다시 안내해드리겠습니다.

## 지금 상태

- 로그인 화면(`/dashboard/login`, `/mobile/join`)에 카카오·네이버·구글 버튼이 이미 떠 있습니다.
- 네이버/구글 버튼을 누르면 아직 환경변수가 없어서 "서버에 네이버(구글) 로그인 환경변수가 아직 설정되지 않았습니다" 오류 화면으로 돌아옵니다. 아래 순서대로 설정하면 정상 동작합니다.
- Supabase에 `naver_user_id`, `google_user_id` 컬럼을 추가하는 마이그레이션도 아직 실행되지 않았습니다. (3단계에서 안내)

---

## 1. 네이버 로그인 설정

### 1-1. 네이버 개발자센터에서 애플리케이션 등록

1. https://developers.naver.com 접속 후 네이버 아이디로 로그인합니다.
2. 상단 메뉴에서 `Application` > `애플리케이션 등록`을 클릭합니다.
3. 애플리케이션 이름: `MAJU Intelligence` (원하는 이름으로 입력 가능)
4. 사용 API: `네이버 로그인`을 체크합니다.
5. 제공 정보 선택: 최소 `이메일`, `이름`을 선택합니다. (프로필 사진은 선택)
6. 로그인 오픈 API 서비스 환경: `WEB 설정`을 추가합니다.
   - 서비스 URL: `https://maju-intelligence.vercel.app`
   - 네이버 로그인 Callback URL: `https://maju-intelligence.vercel.app/api/auth/naver/callback`
7. 등록을 완료하면 `Client ID`와 `Client Secret`이 발급됩니다. 이 두 값을 복사해둡니다.

### 1-2. Vercel에 환경변수 등록

Vercel 프로젝트 > Settings > Environment Variables에서 아래 3개를 추가합니다 (Production 환경 포함).

```
NAVER_CLIENT_ID=발급받은 Client ID
NAVER_CLIENT_SECRET=발급받은 Client Secret
NAVER_REDIRECT_URI=https://maju-intelligence.vercel.app/api/auth/naver/callback
```

저장 후 재배포(Redeploy)합니다.

---

## 2. 구글 로그인 설정

### 2-1. Google Cloud Console에서 OAuth 클라이언트 만들기

1. https://console.cloud.google.com 접속 후 구글 계정으로 로그인합니다.
2. 새 프로젝트를 만들거나 기존 프로젝트를 선택합니다. (프로젝트 이름 예: `maju-intelligence`)
3. 좌측 메뉴 `API 및 서비스` > `OAuth 동의 화면`으로 이동합니다.
   - User Type: `외부(External)` 선택
   - 앱 이름: `MAJU Intelligence`
   - 사용자 지원 이메일, 개발자 연락처 이메일: 본인 이메일 입력
   - 범위(Scopes)는 기본값(`email`, `profile`, `openid`)이면 충분합니다.
   - 테스트 단계에서는 "테스트 사용자"에 로그인할 구글 계정을 추가해야 로그인 테스트가 가능합니다. (배포 상태를 "프로덕션"으로 전환하면 모든 구글 계정이 사용 가능해집니다.)
4. 좌측 메뉴 `API 및 서비스` > `사용자 인증 정보`로 이동합니다.
5. `+ 사용자 인증 정보 만들기` > `OAuth 클라이언트 ID`를 클릭합니다.
6. 애플리케이션 유형: `웹 애플리케이션`
7. 승인된 리디렉션 URI에 아래 URL을 추가합니다.
   - `https://maju-intelligence.vercel.app/api/auth/google/callback`
8. 만들기를 누르면 `클라이언트 ID`와 `클라이언트 보안 비밀(Client Secret)`이 발급됩니다. 이 두 값을 복사해둡니다.

### 2-2. Vercel에 환경변수 등록

```
GOOGLE_CLIENT_ID=발급받은 클라이언트 ID
GOOGLE_CLIENT_SECRET=발급받은 클라이언트 보안 비밀
GOOGLE_REDIRECT_URI=https://maju-intelligence.vercel.app/api/auth/google/callback
```

저장 후 재배포(Redeploy)합니다.

---

## 3. Supabase 마이그레이션 실행

Supabase 프로젝트 > SQL Editor에서 이 저장소의 `supabase/migrations/20260817_app_users_naver_google_columns.sql` 내용을 그대로 붙여넣고 실행합니다. (카카오 로그인 때 겪었던 것과 같은 "42P10 unique constraint" 오류를 미리 방지하기 위한 마이그레이션입니다.)

---

## 4. 테스트

1. `/dashboard/login` 또는 직원 초대 링크(`/mobile/join?invite=...`)에 접속합니다.
2. 네이버/구글 버튼을 눌러 로그인이 정상적으로 진행되는지 확인합니다.
3. 오류가 나면 화면에 표시되는 문구를 캡처해서 보내주세요. (카카오 때처럼 Redirect URI 등록 위치나 승인 상태 문제일 가능성이 높습니다.)

## 참고: 나중에 살펴볼 부분

- 구글은 OAuth 동의 화면이 "테스트" 상태면 등록한 테스트 계정만 로그인할 수 있습니다. 실제 직원들이 사용하려면 동의 화면을 "프로덕션"으로 게시해야 하며, 앱 로고·개인정보처리방침 URL 등 추가 정보가 필요할 수 있습니다.
- 네이버는 별도 심사 없이 바로 사용 가능하지만, 제공 정보(이메일 등)를 "필수"로 설정해야 로그인 시 이메일을 받아올 수 있습니다.
