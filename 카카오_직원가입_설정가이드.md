# 직원 카카오 간편가입 · PC/모바일 연동 설정 가이드

이 문서는 카카오톡으로 직원을 초대하고, 그 직원이 모바일과 PC 양쪽에서 MAJU Intelligence를 쓸 수 있게 만드는 설정 절차입니다. 코드 쪽 준비(초대 링크 생성, 로그인 세션, PC/모바일 전환 버튼)는 이미 끝나 있고, 남은 건 카카오 로그인 자체를 "켜는" 작업입니다. 이건 사용자님의 카카오 계정으로 직접 진행해야 하는 부분이라 순서대로 안내해 드립니다.

## 0. 진행 전 확인

이미 카카오맵을 지도 화면에 쓰고 계셔서 `KAKAO_REST_KEY`가 등록된 카카오 앱이 하나 있을 가능성이 높습니다. 있다면 새로 앱을 만들 필요 없이 **그 앱에 카카오 로그인 기능만 추가**하면 됩니다. 없다면 1번부터 새로 만들면 됩니다.

## 1. 카카오 디벨로퍼스에서 앱 준비

1. https://developers.kakao.com 접속 → 카카오 계정으로 로그인
2. 상단 "내 애플리케이션" 클릭
3. 기존에 지도용으로 만든 앱이 있으면 그 앱을 선택, 없으면 "애플리케이션 추가하기"로 새로 생성 (앱 이름: MAJU Intelligence 정도면 충분)
4. 앱 선택 후 왼쪽 메뉴에서 **앱 설정 > 요약 정보**로 이동 → "REST API 키" 값을 복사해 둡니다. 이 값이 곧 `KAKAO_CLIENT_ID`입니다.

## 2. 카카오 로그인 활성화

1. 왼쪽 메뉴 **제품 설정 > 카카오 로그인** 클릭
2. "카카오 로그인 활성화" 스위치를 ON으로 변경
3. 같은 화면의 **Redirect URI** 항목에 아래 주소를 정확히 등록합니다 (오타나 슬래시 하나만 달라도 로그인이 실패합니다):

   ```
   https://maju-intelligence.vercel.app/api/auth/kakao/callback
   ```

4. 저장

## 3. 동의항목(수집 정보) 설정

1. 왼쪽 메뉴 **제품 설정 > 카카오 로그인 > 동의항목**
2. "닉네임"을 **필수 동의**로 설정 (직원 이름 표시에 사용)
3. "카카오계정(이메일)"을 **선택 동의**로 설정 (없어도 자동으로 임시 이메일이 생성되니 필수는 아닙니다)
4. 프로필 사진은 선택사항 — 켜두면 직원 아바타로 씁니다

## 4. (선택) Client Secret 발급

1. 왼쪽 메뉴 **제품 설정 > 카카오 로그인 > 보안**에서 "Client Secret 코드 생성" 가능
2. 생성했다면 "사용함"으로 활성화하고 값을 복사해 둡니다 — 이 값은 `KAKAO_CLIENT_SECRET`입니다
3. 필수는 아닙니다. 생성하지 않으면 이 환경변수는 그냥 비워두면 됩니다.

## 5. Vercel에 환경변수 등록

1. https://vercel.com 로그인 → maju-intelligence 프로젝트 선택
2. **Settings > Environment Variables**로 이동
3. 아래 3개(또는 2개)를 추가:

   | 변수명 | 값 |
   |---|---|
   | `KAKAO_CLIENT_ID` | 1번에서 복사한 REST API 키 |
   | `KAKAO_REDIRECT_URI` | `https://maju-intelligence.vercel.app/api/auth/kakao/callback` |
   | `KAKAO_CLIENT_SECRET` | (선택) 4번에서 생성한 값 |

4. Environment는 **Production**에 체크 (필요하면 Preview/Development도 함께)
5. 저장 후 **Deployments** 탭에서 최신 배포를 "Redeploy" — 환경변수는 재배포해야 반영됩니다

## 6. 직원 초대 링크 만들기 (우리 앱)

1. PC로 고객사 계정 로그인 → 좌측 사이드바 하단 **출발지 설정**(`/dashboard/settings`) 이동
2. "직원 초대와 모바일 가입을 관리합니다" 섹션에서 오른쪽 "직원 초대 만들기"에 이름·연락처(선택)·역할(배송기사/영업직원/관리자/직원 중 선택) 입력 후 생성
3. 생성된 초대 카드에서 "복사" 버튼으로 링크를 복사해 카카오톡으로 직원에게 전달

## 7. 테스트

1. 직원 본인 휴대폰에서 카카오톡으로 받은 링크를 엽니다
2. "카카오로 직원 가입" 버튼이 노란색 카카오 로그인 버튼으로 정상 표시되는지 확인 (지금은 "카카오 OAuth 설정 필요" 경고가 뜨는데, 위 설정을 마치면 이 버튼으로 바뀝니다)
3. 카카오 로그인 진행 → 자동으로 "오늘 코스" 화면으로 이동하면 성공
4. 화면 상단의 "PC 버전(전체 화면)으로 보기"를 눌러 PC 대시보드로도 정상 진입되는지 확인
5. 관리자 화면(`/admin/companies`)에서도 해당 직원의 초대 상태가 "수락됨"으로 바뀌었는지 확인 가능합니다

## 자주 나는 에러

- **KOE006 / redirect_uri mismatch**: 2번에서 등록한 Redirect URI와 `KAKAO_REDIRECT_URI` 환경변수 값이 한 글자라도 다른 경우입니다. 둘을 다시 비교하세요.
- **버튼이 계속 회색 경고로만 뜸**: Vercel 재배포를 안 했거나, 환경변수를 Production 환경에 체크하지 않은 경우입니다.
- **로그인은 되는데 오늘 코스가 비어 있음**: 정상입니다. 거래처 관리에서 해당 직원 이름을 담당자로 배정해야 개인 코스가 채워집니다.

---

여기까지 완료되면 알려주세요. 라이브에서 실제 초대 링크로 끝까지 테스트해서 정상 동작하는지 같이 확인하겠습니다.
