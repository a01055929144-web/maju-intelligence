# 거래처 리뷰 자동 수집 파이프라인 설계

작성일: 2026-08-18 · 대상: 거래처 카드 리뷰 요약·키워드 뱃지 기능(이미 배포됨, 현재는 수동 입력만 지원)

## 1. 배경

거래처 상세 패널에 "리뷰 요약 · 키워드 (AI)" 섹션이 이미 배포되어 있고, 스키마(`review_summary`, `review_keywords`, `review_source`, `reviews_updated_at`)도 준비돼 있습니다. 지금은 담당자가 직접 리뷰를 읽고 요약·키워드를 입력하는 방식만 지원합니다. 이 문서는 "네이버플레이스·카카오맵·구글 리뷰를 자동으로 수집해서 채워넣는" 상시 파이프라인을 실제로 구축한다면 어떤 선택지가 있고, 각각의 비용·리스크·구현 난이도가 무엇인지 정리합니다.

## 2. 채널별 현실적인 제약

| 채널 | 공식 API로 리뷰 수집 가능? | 비고 |
| --- | --- | --- |
| 구글 | **가능** — Google Places API(Place Details)가 장소당 최신/관련도순 리뷰 최대 5개를 공식 제공 | 유료(호출당 과금), 승인 절차 없음, ToS 위반 리스크 없음 |
| 네이버플레이스 | 불가 — 네이버는 robots.txt·이용약관으로 자동 수집을 막고 있고, 검색 오픈 API는 블로그·카페·뉴스 검색용이라 플레이스 리뷰는 다루지 않음 | 리뷰를 가져오려면 화면을 직접 파싱(스크래핑)해야 함 → ToS 위반 소지, 차단 리스크 |
| 카카오맵 | 불가 — 카카오 로컬 API는 장소 검색·좌표 변환용이고 리뷰 데이터는 제공하지 않음 | 마찬가지로 스크래핑 필요 |

즉 "3개 채널 모두 자동 수집"을 하려면 구글은 공식 API로 깔끔하게 되지만, 네이버·카카오는 어떤 방식으로든 화면을 직접 읽는 브라우저 자동화(스크래핑)가 필요합니다. 이는 각 서비스 이용약관과 충돌할 수 있고, 계정·IP 차단이나 캡차 대응 같은 운영 부담이 따릅니다.

## 3. 아키텍처 옵션 비교

### 옵션 A. 온디맨드 수동 트리거 (제가 지금 하고 있는 방식)
Claude가 Chrome으로 특정 거래처의 리뷰를 직접 열람·요약해서 저장. 요청할 때마다 실행.

- 장점: 추가 인프라 없음, 지금 당장 가능, 각 거래처마다 사람이 결과를 확인 가능
- 단점: 완전 자동이 아님(요청해야 실행됨), 거래처가 많아지면 비효율적
- 비용: 0원 (기존 대화 내 작업)
- 리스크: 낮음 — 사람이 매번 결과를 검수하므로 잘못된 데이터가 쌓일 위험이 작음

### 옵션 B. 구글 리뷰만 공식 API로 상시 자동화
신규/기존 거래처 저장 시 서버가 Google Places API를 호출해 리뷰 최대 5개를 가져오고, LLM(예: Claude Haiku)으로 요약·키워드를 생성해 자동 저장. 네이버·카카오는 계속 수동 입력.

- 아키텍처: `upsertCustomerMaster` 저장 성공 후 비동기로 `POST /api/reviews/sync` 같은 내부 라우트 호출 → Google Places Text Search로 place_id 확인 → Place Details(reviews 필드) 조회 → LLM 요약 호출 → `review_summary`/`review_keywords`/`review_source`/`reviews_updated_at` 업데이트
- 장점: 100% 합법적인 공식 API만 사용, 헤드리스 브라우저 인프라 불필요, 구현이 상대적으로 단순, Vercel 서버리스 함수로 충분
- 단점: 구글 리뷰가 없거나 적은 매장(특히 개인 식당)은 데이터가 빈약할 수 있음. 네이버가 한국에서 더 많이 쓰이는 채널이라 커버리지가 아쉬울 수 있음
- 비용: Google Places API 호출당 과금 (Place Details 호출 기준 대략 건당 몇 십원~백원대, 거래처 수가 많지 않은 현재 규모(7곳)에서는 사실상 무시할 수준). 매달 소액의 무료 크레딧도 제공됨
- 리스크: 낮음 (ToS 준수, 차단 위험 없음)
- 구현 난이도: 낮음~중간 (API 키 발급 + 서버 라우트 하나 추가 수준)

### 옵션 C. 헤드리스 브라우저 상시 파이프라인 (네이버·카카오 포함, 사용자가 원래 언급한 "브라우저 자동화")
Vercel 서버리스 함수에서 `puppeteer-core` + `@sparticuz/chromium`으로 네이버플레이스·카카오맵 페이지를 직접 열어 리뷰를 파싱. 거래처 등록/주기적 배치(cron)로 실행.

- 장점: 사용자가 원래 그렸던 "네이버·카카오·구글 리뷰 모두 자동 정리"에 가장 가까움
- 단점:
  - 네이버·카카오 이용약관 위반 소지 (계정 정지·법적 리스크는 낮지만 0은 아님)
  - Vercel 서버리스 함수 번들 크기 제한(~50MB) 때문에 `puppeteer-core` + 별도 서버리스 크로미움 바이너리 조합이 필수이고, 콜드스타트 시 몇 초씩 걸림
  - 페이지 구조가 바뀌면(네이버·카카오가 마크업을 자주 바꿈) 파싱 로직이 깨짐 → 지속적인 유지보수 필요
  - IP 차단·캡차 대응 로직(프록시, 재시도, 속도 제한)이 추가로 필요해질 수 있음
  - 실행 시간이 길어질 수 있어 Vercel 함수 실행시간 제한(플랜별 상이)에 걸릴 가능성
- 비용: 코드 자체는 무료지만, 실행 시간이 늘어나면 Vercel 함수 실행 시간 과금이 늘어날 수 있고, 차단 회피용 프록시 서비스를 쓰면 별도 월 비용 발생
- 리스크: 중간~높음 (ToS, 차단, 유지보수 부담)
- 구현 난이도: 높음 (별도의 스코프된 작업으로 진행 권장)

### 옵션 D. 하이브리드 (권장)
1. 구글은 옵션 B로 지금 바로 상시 자동화
2. 네이버·카카오는 당분간 옵션 A(온디맨드 수동 트리거, 담당자가 "리뷰 갱신해줘" 요청 시 Claude가 처리) 유지
3. 거래처 수가 늘어나고 네이버·카카오 커버리지가 실제로 아쉬워지는 시점에 옵션 C를 별도 프로젝트로 재검토

## 4. 권장안 요약

지금 규모(거래처 7곳, 계속 늘어나는 중)에서는 **옵션 D(하이브리드)**를 권장합니다.

- 구글 리뷰 자동화(옵션 B)는 리스크가 거의 없고 지금 바로 착수 가능한 작업이라 우선순위를 높게 잡을 만합니다.
- 네이버·카카오 자동화(옵션 C)는 ToS 리스크·유지보수 부담이 커서, "지금 몇 곳 채워넣기" 이상의 상시 운영을 하려면 별도 스코프(전용 인프라 투자 결정)로 다시 논의하는 게 맞습니다.

## 5. 구현 완료 (2026-08-18) — 남은 건 API 키뿐

옵션 B를 실제로 구현했습니다. 코드는 이미 배포됐고, **`GOOGLE_PLACES_API_KEY` 환경변수만 설정하면 바로 작동합니다.**

- `lib/google-reviews.ts`: Google Places API(Text Search + Place Details)로 리뷰를 가져오고, LLM 없이 규칙 기반(빈도 분석)으로 요약·키워드를 만드는 `syncGoogleReviewsForCustomer()`. API 키가 없으면 항상 안전하게 no-op으로 끝납니다.
- `lib/store.ts`: `syncCustomerGoogleReviews()`(거래처 1곳 즉시 새로고침), `refreshCustomerGoogleReviews()`/`refreshAllCompaniesGoogleReviews()`(오래되었거나 없는 순으로 회사당 하루 최대 30곳씩 자동 새로고침).
- **신규 거래처 등록 시 자동 실행**: `/api/customers` POST로 새 거래처를 저장하면, 리뷰를 직접 입력하지 않은 경우 백그라운드로 구글 리뷰를 자동 수집합니다.
- **매일 새벽 cron 자동 재동기화**: 기존 `/api/cron/business-status`(사업자 상태 조회와 같은 Vercel Hobby cron 슬롯 공유)에서 함께 실행되어, 리뷰가 없거나 오래된 거래처를 매일 조금씩 채웁니다. 담당자가 직접 조사해서 입력한 리뷰(구글 자동 수집이 아닌 출처)는 이 자동 새로고침이 덮어쓰지 않습니다.
- **수동 새로고침 버튼**: 거래처 상세 패널 "리뷰 요약 · 키워드" 섹션에 "구글 리뷰 새로고침" 버튼을 추가해, 담당자가 원할 때 즉시 다시 수집할 수 있습니다(이쪽은 출처와 무관하게 항상 덮어씁니다).
- 네이버플레이스·카카오맵은 여전히 수동 입력입니다(2절에서 설명한 ToS 제약 때문 — 변경 없음).

### 남은 사용자 액션

1. [Google Cloud Console](https://console.cloud.google.com)에서 프로젝트를 만들고 "Places API"를 활성화합니다(결제 계정 연결 필요 — 매달 소액 무료 크레딧이 제공되고, 지금 규모(거래처 7곳)에서는 사실상 무료입니다).
2. API 키를 발급받아 Vercel 프로젝트 환경변수에 `GOOGLE_PLACES_API_KEY`로 등록하고 재배포합니다.
3. 그 다음부터는 신규 거래처는 자동으로, 기존 거래처는 매일 cron으로 순차적으로 채워집니다. 급하면 거래처 상세 패널의 "구글 리뷰 새로고침" 버튼으로 즉시 실행할 수 있습니다.

## 6. 옵션 C(서버 상시 크롤러) 재검토 결과 — 채택하지 않음 (2026-08-18)

네이버·카카오까지 "완전 자동"으로 만들기 위해 옵션 C(헤드리스 브라우저 상시 크롤러)를 다시 검토했으나, 실제로 시도하는 과정에서 명확한 차단 신호를 확인해 채택하지 않기로 했습니다.

- map.naver.com은 자동화된 콘텐츠 수집을 정책적으로 차단하는 대상으로 확인되었습니다(Claude의 브라우징 도구 자체가 이 도메인 접근을 안전 정책상 거부함). 이는 "차단될 수도 있다"는 리스크 수준이 아니라, 이 도메인을 자동으로 반복 접근하는 인프라(서버 크롤러 포함)를 만드는 것 자체가 정책적으로 부적절하다는 뜻입니다.
- 유료 상품으로 판매할 계획이라는 점도 이 판단을 강화합니다 — 크롤러가 차단되면 여러 유료 고객사의 기능이 동시에 멈추는 단일 장애점이 되고, 고객사 수가 늘수록 계정/IP 차단 확률도 커집니다.
- 카카오맵은 Claude 도구에서 접근이 막혀 있지는 않았지만, 공식 리뷰 API가 없다는 점과 이용약관 리스크는 동일하게 남아 있어 마찬가지로 서버 크롤러 대상에서 제외했습니다.

## 7. 대신 채택한 방식 — "붙여넣기 → AI 자동 요약" (구현 완료, 2026-08-18)

네이버·카카오는 완전 무인 자동화 대신, 사람이 직접 읽은 리뷰 텍스트를 넣으면 그 자리에서 AI가 즉시 요약하는 방식으로 구현했습니다. 스크래핑이 전혀 없어 위 리스크와 무관하고, 상용 SaaS로 파는 데도 문제가 없습니다.

- `lib/review-summarizer.ts`: 리뷰 텍스트(구글 API로 가져온 것이든, 사람이 붙여넣은 것이든)를 규칙 기반(빈도 분석)으로 요약·키워드화하는 공용 로직. 구글 자동 수집(`lib/google-reviews.ts`)과 로직을 공유합니다.
- `lib/store.ts`의 `summarizeCustomerReviewText()`: 어떤 URL도 스스로 가져오지 않고, 담당자가 이미 복사해서 넘긴 텍스트만 처리합니다.
- `POST /api/customers/[id]/summarize-reviews`: 위 함수를 호출하는 API 라우트.
- **UI**: 거래처 상세 패널 "리뷰 요약 · 키워드" 섹션에 네이버/카카오/기타 출처 선택 + 리뷰 원문 붙여넣기 칸 + "AI 요약 생성" 버튼 한 번으로, 담당자가 링크를 열어 리뷰를 읽고 텍스트를 복사해온 뒤 즉시 요약·키워드가 생성됩니다.
- 거래처 등록 시 네이버·카카오 링크는 지금처럼 계속 자동 생성됩니다(URL 생성 자체는 문제없음) — 담당자가 그 링크를 눌러 리뷰를 열람하는 용도로 이어집니다.

담당자 입장에서는 "복사 → 붙여넣기 → 버튼 클릭" 세 동작만으로 요약이 완성되어, 직접 요약 문장을 쓰는 것보다 훨씬 빠르고 자동화된 느낌을 줍니다.

## 8. 향후 검토 사항

- 요약 품질을 더 높이고 싶다면, 현재의 규칙 기반(빈도 분석) 요약기를 LLM(Anthropic/OpenAI) 기반으로 교체할 수 있습니다 — 이 경우 별도의 LLM API 키가 추가로 필요하며, 비용은 거래처 수·갱신 빈도에 비례해 늘어납니다. 지금은 추가 비용 없이 규칙 기반으로 운영하기로 결정했습니다.
- 네이버가 향후 공식 리뷰 API를 제공하게 되면(현재는 없음, 2026-08-18 기준 확인) 구글과 동일한 방식(옵션 B)으로 전환할 수 있도록 설계를 열어뒀습니다.
