# 거래처 리뷰 자동 수집 파이프라인 설계

작성일: 2026-08-18 · 대상: 거래처 카드 리뷰 요약·키워드 뱃지 기능(이미 배포됨, 현재는 수동 입력만 지원)

## 1. 배경

거래처 상세 패널에 "리뷰 요약 · 키워드 (AI)" 섹션이 이미 배포되어 있고, 스키마(`review_summary`, `review_keywords`, `review_source`, `reviews_updated_at`)도 준비돼 있습니다. 지금은 담당자가 직접 리뷰를 읽고 요약·키워드를 입력하는 방식만 지원합니다. 이 문서는 "네이버플레이스·카카오맵·구글 리뷰를 자동으로 수집해서 채워넣는" 상시 파이프라인을 실제로 구축한다면 어떤 선택지가 있고, 각각의 비용·리스크·구현 난이도가 무엇인지 정리합니다.

## 2. 채널별 현실적인 제약

| 채널 | 공식 API로 리뷰 수집 가능? | 비고 |
| --- | --- | --- |
| 구글 | **가능** — Google Places API(Place Details)가 장소당 최신/관련도순 리뷰 최대 5개를 공식 제공 | 유료(호출당 과금), 승인 절차 없음, ToS 위반 리스크 없음 |
| 네이버플레이스 | 불가 — 네이버는 robots.txt·이용약관으로 자동 수집을 막고 있고, 검색 오픈 API는 블로그·카페·뉴스 검색용이라 플레이스 리뷰는 다루지 않음 | 리뷰를 가져오려면 화면을 직접 파싱(스크래핑)해야 함 → ToS 위반 소지, 차단 리스크 |
| 카카오맵 | 불가 — 카카오 로컬 API는 장소 검색·좌표 변환용이고 리뷰 데이터는 제공하지 않음 | 마찬가지로 스크래핑 필요 |

즉 "3개 채널 모두 자동 수집"을 하려면 구글은 공식 API로 깔끔하게 되지만, 네이버·카카오는 어떤 방식으로든 화면을 직접 읽는 브라우저 자동화(스크래핑)가 필요합니다. 이는 각 서비스 이용약관과 충돌할 수 있고, 계정·IP 차단이나 캡차 대응 같은 운영 부담이 따릅니다.

## 3. 아키텍처 옵션 비교

### 옵션 A. 온디맨드 수동 트리거 (제가 지금 하고 있는 방식)
Claude가 Chrome으로 특정 거래처의 리뷰를 직접 열람·요약해서 저장. 요청할 때마다 실행.

- 장점: 추가 인프라 없음, 지금 당장 가능, 각 거래처마다 사람이 결과를 확인 가능
- 단점: 완전 자동이 아님(요청해야 실행됨), 거래처가 많아지면 비효율적
- 비용: 0원 (기존 대화 내 작업)
- 리스크: 낮음 — 사람이 매번 결과를 검수하므로 잘못된 데이터가 쌓일 위험이 작음

### 옵션 B. 구글 리뷰만 공식 API로 상시 자동화
신규/기존 거래처 저장 시 서버가 Google Places API를 호출해 리뷰 최대 5개를 가져오고, LLM(예: Claude Haiku)으로 요약·키워드를 생성해 자동 저장. 네이버·카카오는 계속 수동 입력.

- 아키텍처: `upsertCustomerMaster` 저장 성공 후 비동기로 `POST /api/reviews/sync` 같은 내부 라우트 호출 → Google Places Text Search로 place_id 확인 → Place Details(reviews 필드) 조회 → LLM 요약 호출 → `review_summary`/`review_keywords`/`review_source`/`reviews_updated_at` 업데이트
- 장점: 100% 합법적인 공식 API만 사용, 헤드리스 브라우저 인프라 불필요, 구현이 상대적으로 단순, Vercel 서버리스 함수로 충분
- 단점: 구글 리뷰가 없거나 적은 매장(특히 개인 식당)은 데이터가 빈약할 수 있음. 네이버가 한국에서 더 많이 쓰이는 채널이라 커버리지가 아쉬울 수 있음
- 비용: Google Places API 호출당 과금 (Place Details 호출 기준 대략 건당 몇 십원~백원대, 거래처 수가 많지 않은 현재 규모(7곳)에서는 사실상 무시할 수준). 매달 소액의 무료 크레딧도 제공됨
- 리스크: 낮음 (ToS 준수, 차단 위험 없음)
- 구현 난이도: 낮음~중간 (API 키 발급 + 서버 라우트 하나 추가 수준)

### 옵션 C. 헤드리스 브라우저 상시 파이프라인 (네이버·카카오 포함, 사용자가 원래 언급한 "브라우저 자동화")
Vercel 서버리스 함수에서 `puppeteer-core` + `@sparticuz/chromium`으로 네이버플레이스·카카오맵 페이지를 직접 열어 리뷰를 파싱. 거래처 등록/주기적 배치(cron)로 실행.

- 장점: 사용자가 원래 그렸던 "네이버·카카오·구글 리뷰 모두 자동 정리"에 가장 가까움
- 단점:
  - 네이버·카카오 이용약관 위반 소지 (계정 정지·법적 리스크는 낮지만 0은 아님)
  - Vercel 서버리스 함수 번들 크기 제한(~50MB) 때문에 `puppeteer-core` + 별도 서버리스 크로미움 바이너리 조합이 필수이고, 콜드스타트 시 몇 초씩 걸림
  - 페이지 구조가 바뀌면(네이버·카카오가 마크업을 자주 바꿈) 파싱 로직이 깨짐 → 지속적인 유지보수 필요
  - IP 차단·캡차 대응 로직(프록시, 재시도, 속도 제한)이 추가로 필요해질 수 있음
  - 실행 시간이 길어질 수 있어 Vercel 함수 실행시간 제한(플랜별 상이)에 걸릴 가능성
- 비용: 코드 자체는 무료지만, 실행 시간이 늘어나면 Vercel 함수 실행 시간 과금이 늘어날 수 있고, 차단 회피용 프록시 서비스를 쓰면 별도 월 비용 발생
- 리스크: 중간~높음 (ToS, 차단, 유지보수 부담)
- 구현 난이도: 높음 (별도의 스코프된 작업으로 진행 권장)

### 옵션 D. 하이브리드 (권장)
1. 구글은 옵션 B로 지금 바로 상시 자동화
2. 네이버·카카오는 당분간 옵션 A(온디맨드 수동 트리거, 담당자가 "리뷰 갱신해줘" 요청 시 Claude가 처리) 유지
3. 거래처 수가 늘어나고 네이버·카카오 커버리지가 실제로 아쉬워지는 시점에 옵션 C를 별도 프로젝트로 재검토

## 4. 권장안 요약

지금 규모(거래처 7곳, 계속 늘어나는 중)에서는 **옵션 D(하이브리드)**를 권장합니다.

- 구글 리뷰 자동화(옵션 B)는 리스크가 거의 없고 지금 바로 착수 가능한 작업이라 우선순위를 높게 잡을 만합니다.
- 네이버·카카오 자동화(옵션 C)는 ToS 리스크·유지보수 부담이 커서, "지금 몇 곳 채워넣기" 이상의 상시 운영을 하려면 별도 스코프(전용 인프라 투자 결정)로 다시 논의하는 게 맞습니다.

## 5. 옵션 B를 진행할 경우 실행 단계 (참고용)

1. Google Cloud Console에서 Places API 활성화, API 키 발급 (사용자가 직접 진행 — API 키 발급은 계정 소유자만 가능)
2. `lib/store.ts`에 `syncGoogleReviews(customerId)` 함수 추가: Places Text Search로 place_id 조회 → Place Details(`fields=reviews`) 호출 → 리뷰 텍스트를 모아 LLM 요약 요청 → `review_summary`/`review_keywords`/`review_source: "구글"`/`reviews_updated_at` 저장
3. 거래처 저장(`/api/customers` POST/PATCH) 성공 후 또는 별도 "리뷰 새로고침" 버튼에서 위 함수를 비동기 호출
4. (선택) Vercel Cron으로 주간 1회 전체 거래처 재동기화

## 6. 다음 결정이 필요한 지점

- 옵션 B(구글 자동화)부터 바로 착수할지, 아니면 지금처럼 온디맨드 수동 방식을 계속 쓸지
- 착수한다면 Google Cloud 프로젝트/API 키를 사용자가 준비해줄 수 있는지
