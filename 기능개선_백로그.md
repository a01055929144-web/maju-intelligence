# 기능 개선 백로그

바로 진행하기보다 아이디어를 모아두고 우선순위를 정해서 진행하기 위한 문서입니다. 새 요청이 생기면 아래에 이어서 추가합니다.

---

## 1. 지도 홈 검색 - 미등록 매장까지 확장 (요청일 2026-08-17)

### 현재 상태

`/dashboard` 지도 홈 상단 검색창("거래처명·지역·주소 검색...")은 이미 등록된 거래처(고객사 DB) 안에서만 검색됩니다. 아직 거래처로 등록하지 않은 주변 매장은 지도 홈에서 검색해도 나오지 않습니다.

### 요청 내용

1. **검색 범위 확장**: 지도 홈에서 검색하면 기존 거래처뿐 아니라 아직 등록되지 않은 주변 매장도 결과에 나왔으면 합니다. 목적은 사용성 극대화 및 영업 효율성 향상입니다.
2. **검색 → 즉시 등록 흐름**: 검색 결과에서 미등록 매장을 선택하면 그 자리에서 바로 거래처로 등록할 수 있어야 합니다.
3. **등록 후 서류 업로드 연결**: 등록한 뒤 사업자등록증, 신분증, 적재위치 사진 등을 업로드해서 바로 사용할 수 있도록 연결되어야 합니다.
4. **모바일 대응**: 모바일에서도 PC와 동일하게 쉽게 검색·등록하고 사용성을 극대화해야 합니다.

### 참고할 수 있는 기존 구현

- 신규영업 리드 추천에는 이미 카카오 로컬 API 기반 실제 매장 검색이 붙어있습니다(과거 작업: "Replace synthetic leads with real Kakao Local API businesses"). 지도 홈 검색에도 같은 패턴을 재사용할 수 있을 것으로 보입니다.
- 거래처 등록(데이터 등록) 화면에는 이미 사업자등록증/신분증 첨부, 적재위치 사진 업로드 기능이 있습니다. 미등록 매장을 "빠른 등록"할 때 이 플로우로 자연스럽게 이어붙이면 될 것으로 보입니다.
- 사업자명 검색·자동완성 기능도 이미 데이터 등록 화면에 있어 유사한 UX를 지도 홈에도 적용할 수 있습니다.

### 예상 범위

중~대형. 지도 검색 로직 확장, 미등록 매장을 거래처와 구분해 지도에 표시하는 마커 스타일, "빠른 등록" 모달/플로우 설계, 모바일(`app/mobile`) 화면 대응까지 포함하면 여러 화면에 걸친 작업이 될 것으로 예상됩니다.

### 진행 상황 (2026-08-17)

- **완료 (PC)**: 지도 홈 검색창에 카카오맵 실제 매장 검색 결과가 함께 뜨도록 확장했습니다(항목 1). 이미 등록된 거래처와 이름이 겹치는 결과는 자동으로 제외합니다. 검색 결과의 "거래처로 등록" 버튼을 누르면 데이터 등록 화면의 수기 입력 폼이 상호명·주소·연락처·업종이 채워진 채로 바로 열립니다(항목 2). 라이브에서 "스타벅스" 검색 → "거래처로 등록" → 폼 자동 채움까지 확인 완료.
- **완료 (모바일, 항목 4)**: `/mobile/register` 화면을 새로 만들었습니다. 카카오맵 매장 검색(기존 `/api/business-search` 재사용) → 결과 선택 또는 "직접 입력" → 상호명만 있으면 바로 저장(`/api/customers`, 사업자번호는 나중에 보완 가능) → 저장 직후 같은 화면에서 사업자등록증·신분증·적재위치 파일 업로드(기존 `/api/customer-attachments/upload` 패턴 재사용)까지 한 흐름으로 이어집니다. 오늘 코스 화면(`/mobile/today`) 하단 탭에 "등록" 메뉴를 추가해 어디서든 접근할 수 있습니다.
- **버그 발견 및 수정 (2026-08-17 추가 점검)**: 위 PC 흐름을 다시 점검하다가 두 가지 실제 문제를 발견해 같이 고쳤습니다.
  1. 수기 등록 화면이 사업자등록번호를 반드시 입력해야만 저장되도록 막혀 있었습니다. 지도에서 찾은 미등록 매장은 사업자등록증이 아직 없는 경우가 많아서, 사실상 "즉시 등록"이 불가능한 상태였습니다. → 사업자등록번호·대표자명을 수기 등록(빠른 등록)에서는 선택 입력으로 바꾸고, 저장 후 서류로 보완할 수 있도록 문구도 함께 정리했습니다. OCR 서류 검토 모드는 서류를 보며 입력하는 화면이라 기존처럼 필수를 유지했습니다.
  2. 데이터 등록 화면의 "첨부자료"(사업자등록증/신분증/통장사본/적재위치) 파일 선택 UI가 파일명만 화면에 표시할 뿐 실제로는 어디에도 업로드되지 않는 상태였습니다(항목 3이 사실은 미완성이었습니다). → 모바일 등록에 쓴 것과 같은 실제 업로드 로직(`/api/customer-attachments/upload`)으로 다시 연결해서, 저장된 거래처에 실제로 파일이 붙도록 고쳤습니다.
  라이브에서 수기 등록 화면에 사업자등록번호/대표자명 입력 없이도 "검증 후 저장" 버튼이 활성화되는 것과 안내 문구가 바뀐 것까지 확인했습니다.
- **지도 메인화면에서 화면 이동 없이 등록 (2026-08-17 추가 요청)**: 검색 결과의 "거래처로 등록" 버튼이 기존에는 데이터 등록 화면으로 이동시켰는데, 지도 화면을 떠나지 않고 그 자리에서 등록까지 끝낼 수 있도록 바꿔달라는 요청이 있었습니다. 지도 홈 오른쪽에서 열리는 "빠른 등록" 패널을 새로 만들어, 검색 결과를 누르면 그 패널에서 상호명·주소·연락처·업종을 확인/수정하고 바로 저장(사업자번호는 여전히 선택 입력)한 뒤, 같은 패널 안에서 사업자등록증/신분증/통장사본/적재위치 파일을 업로드하고 "완료"를 누르면 지도 데이터가 새로고침되어 방금 등록한 거래처가 반영됩니다. 라이브에서 "스타벅스 서울" 검색 → "거래처로 등록" → 패널에서 저장 → 첨부자료 패널 표시 → "완료" → 전체 거래처 수 2개→3개로 반영까지 실제로 한 번 등록해 전 과정을 확인했습니다. 테스트로 등록된 "스타벅스 더북한산점" 항목은 실제 매장 정보로 정상 등록된 것이라 거래처 관리 화면에서 그대로 두거나 필요 없으면 삭제하시면 됩니다.
- **미등록 매장을 지도에 마커로 표시 (2026-08-17 추가 요청)**: 그동안 검색으로 찾은 미등록 매장은 검색창 아래 목록에서만 보였는데, 지도 위에도 바로 보이면 좋겠다는 요청이 있었습니다. 검색 결과 중 아직 등록되지 않은 매장을 지도에 점선 테두리 + "+" 배지가 있는 주황색 마커로 표시하도록 했습니다. 확정된 거래처 마커와 한눈에 구분되고, 이 마커를 클릭하면 검색 결과 목록의 "거래처로 등록" 버튼을 눌렀을 때와 똑같이 "빠른 등록" 패널이 그 자리에서 열립니다. 실제 지도 위 위치는 카카오맵이 주소를 지오코딩해서 찾으므로 별도 좌표 수집 작업 없이 바로 동작합니다. 경유 코스(티맵 연동) 탭에는 영향이 없도록 지도 홈 화면에서만 적용했습니다.

### 상태

**완료** — PC·모바일 모두 검색→즉시등록→서류 업로드 플로우 배포 완료. PC는 지도 화면을 떠나지 않고 등록까지 끝내는 인라인 패널로 완성. 미등록 매장은 지도 위 마커로도 바로 보이고, 마커 클릭으로 등록까지 이어집니다.
